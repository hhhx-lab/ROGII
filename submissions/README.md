# Submissions

This directory is for locally generated Kaggle submission CSV files.

Regenerate them from the tracked scripts after downloading the data locally. Keep filenames stable so leaderboard experiments can be traced back to the exact baseline family that produced them.
