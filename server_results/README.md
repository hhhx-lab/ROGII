# Returned Server Results

This directory is tracked as a placeholder only.

Use it for unpacking Part 2 server result archives locally, for example:

```bash
mkdir -p server_results/part2_full
tar -xzf packages/part2_server_outputs_*.tar.gz -C server_results/part2_full
```

Returned server artifacts are intentionally ignored by git.
