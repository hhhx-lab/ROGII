# Local Transfer Packages

This directory is tracked as a placeholder only.

Server output archives generated by `scripts/package_part2_server_outputs.py` are intentionally ignored by git. Copy them manually between the server and local workstation.
